# App::Sqitch init fixture (mysql)

Source:
- Command: `sqitch init myproj --engine mysql --top-dir . --plan-file sqitch.plan --extension sql`
- App::Sqitch version: `v1.6.1` (sqitch/sqitch:latest)

Notes:
- Captured from a clean working directory.
- Includes the generated `sqitch.conf` and `sqitch.plan` outputs.
- The captured `sqitch.plan` sets `%project=sqitch`, matching the App::Sqitch output from this run.
